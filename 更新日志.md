# 变更记录

## 1.3.2

1、修复：解析地址失败的问题
2、微调了一下UI

## 1.3.1 — 紧急修复版，速更

1、修复赞助者权益不可用的问题
2、修复百度地图规划路径看不见，拖不动等问题
3、改进自动路径但非循环时，达到终点暂停，不再主动停止

## 1.3.0 — 路径模拟与应用兼容性重构

本次更新在原有位置模拟基础上，正式加入路径模拟，并重点优化应用兼容性、地图交互和运行稳定性。

### 新增路径模拟

- 支持在地图上规划多路点路径，并实时展示移动轨迹、速度和当前位置。
- 提供自动与摇杆两种模拟方式，支持速度调节、单程/循环、暂停及长按停止。
- 支持收藏常用路径、后台持续运行，以及可选的画中画状态控制。

### 重构应用兼容性

- 支持对每个已 Hook 应用独立设置坐标系、路径模拟方案和步频模拟策略。
- 支持分别调整系统、高德、百度、腾讯、Google 和华为等定位通道。

### 优化地图与交互

- 位置模拟和路径规划均支持拖动地图或点击地图选点。
- 新增跟随手势的整屏层叠动画，可上滑切换位置模拟与路径模拟。
- 优化规划路径抽屉、按钮反馈、震动交互。
- 优化夜间模式和返回键逻辑。
- 优化冷启动流程，减少启动画面和进入地图主页的等待时间。

### 修复 Mock 定位模式

- 修复部分机型使用 Mock 定位模式时，在开发者选项中找不到 LocusMimic 的问题。
- 修复 Mock 定位模式下位置很快恢复到真实位置的问题；使用该模式时请关闭 Wi-Fi。

## 1.2.3 — 稳定性与赞助记录优化

1. 优化内置地图轮换策略，提升使用内置地图凭证的稳定性
2. 进入赞助页自动刷新赞助状态，新增查看提交的历史记录
3. 优化地图会话切换稳定性。

## 1.2.2 — 坐标兼容与地图体验优化

1. 新增应用坐标系配置，改善部分应用的定位偏移问题，如威信、每团等系列应用。
2. 新增日间、夜间及跟随系统的主题模式。
3. 优化地图启动、渲染速度和移动动画。
4. 重构 OpenStreetMap，减小安装包体积。
5. 优化启动速度、抽屉动画及界面细节。
6. 修复多项地图加载、搜索和状态恢复问题。

注意：修改坐标系后，请重新冷启动目标应用。默认地图需要“科学上网”。

## 1.2.0 — 地图服务与界面优化

由于开发者个人认证额度有限，无法长期承担大家公用的消耗，本版本起不再默认提供公共地图凭证，旧版内置凭证也将逐步失效。
但现提供无需申请 Key 的免费 OSM 地图；你也可以配置自己的百度、高德或 Google 地图凭证，位置模拟基础功能不受影响。

1. 新增坐标定位：可手动输入经纬度快速选择位置。
2. 新增免费 OpenStreetMap，并设为默认地图服务。
3. 移除内置百度地图 AK；新增高德地图与 Google 地图支持，可使用自己的凭证。
4. 新增主动后台资源释放机制，并优化 Hook 配置缓存与 Mock Provider 生命周期管理，显著降低后台资源占用与耗电。
5. 优化部分界面的视觉设计、布局与交互细节。

注意：

1. OSM 可能需要科学上网；Google 地图采用按量计费，请使用已启用结算的自有 Key。
2. 为降低后台占用，应用进入后台或退出后会释放地图容器。因此再次回到前台时地图视图会重新创建，短暂白屏属于正常现象。

## 1.1.0 — 性能与能耗优化

- 优化后台运行机制，退至后台后自动释放地图 WebView、脚本及渲染资源。
- 优化地址解析与搜索请求调度，减少连续点选产生的重复网络请求。
- 优化 Hook 配置缓存与按需执行逻辑，降低定位高频回调期间的资源开销。
- 优化 Mock Provider 服务生命周期和熄屏推送频率，减少后台唤醒与耗电。
- 移除原生地图 SDK 和非必要资源，并启用代码、资源压缩。

## 1.0.1 — 地图服务与界面优化

- 新增自定义百度地图 AK，可在默认免费额度不足时切换为用户自行申请的浏览器端（JavaScript API）AK。
- 新增 Telegram 频道入口，方便订阅项目动态与交流探讨。
- 优化部分 UI 交互与视觉效果。

## 1.0.0 — 首次公开发布

- 重新实现更简洁、时尚的地图交互界面，整合地点搜索、收藏、当前位置、位置参数配置与作用应用管理。
- 将地图服务替换为百度地图 Web 版本，支持地图选点与地点搜索。
- 整合 HideMockLocation 的部分实现特性，提升本模块在部分反模拟检测较强应用中的位置模拟隐藏性。
- 提供互斥的应用 Hook、系统 Hook 与 Mock Provider 三种模式。

## 上游说明

本项目在 XposedFakeLocation 的基础上维护，并整合了基于 HideMockLocation 的部分实现思路。早期私人开发记录不作为公开版本历史发布；上游归属与许可证见 [docs/FORK_CHANGES.md](docs/FORK_CHANGES.md) 和 [LICENSE](LICENSE)。
